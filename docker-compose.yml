version: '3.8'

services:
  casa-lago-web:
    build: .
    ports:
      - "3000:80"
    container_name: casa-lago-local
    restart: unless-stopped
    volumes:
      - ./dist:/usr/share/nginx/html:ro
    networks:
      - casa-lago-network

networks:
  casa-lago-network:
    driver: bridge